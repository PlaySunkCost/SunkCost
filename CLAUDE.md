# Sunk Cost — Claude Code entry point

Read and follow the shared repository instructions:

@AGENTS.md

Keep shared rules in `AGENTS.md` and the linked `docs/` files so Claude and Codex
do not maintain competing copies. Paths in the review prompts are relative to the
repository root. `.claude/agents/` contains optional specialized reviewers;
their findings are advice, not approval or authority to change the design.
